describe("User Service Unit Tests", () => {
  describe("Sign Up User functionality", () => {
    it("should successfully add a user if the number of users in the DB with the same profiled is zero", async () => {
      console.log('empty')
    })

    it("should throw an error if the number of users with the same profileId is not zero", async () => {
      console.log('empty')
    })
  })

  describe("Sign In User functionality", () => {
    it("should return a valid jwt token", async () => {
      console.log('empty')
    })
  })
})
